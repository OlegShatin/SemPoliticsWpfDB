<Window x:Class="SemPoliticsWpfDB.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SemPoliticsWpfDB"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000">
    <Grid>
        <TabControl x:Name="tabControl" HorizontalAlignment="Left" Height="536" Margin="10,23,0,0" VerticalAlignment="Top" Width="972">
            <TabItem Header="TabItem" Margin="0,0,-4,-2">
                <Grid Background="#FFE5E5E5"  Margin="10,4,11,51">
                    <ListView x:Name="PastElections" ItemsSource="{Binding PastElectionsList, Mode=TwoWay}" Height="415" Margin="10,20,10,0" VerticalAlignment="Top">
                        <ListView.ItemTemplate>
                            <DataTemplate DataType="viewModels:ElectionViewModel">
                                <Grid >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto"></ColumnDefinition>
                                        <ColumnDefinition Width="auto"></ColumnDefinition>
                                        <ColumnDefinition Width="auto"></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column="1" Content="{Binding MainLabel}"></Label>
                                    <ComboBox Grid.Column="2" x:Name="cb" ItemsSource="{Binding Types}" SelectedValue="{Binding Type}">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="SelectionChanged">
                                                <i:InvokeCommandAction Command="{Binding ChangeType}" 
                                CommandParameter="{Binding SelectedItem, ElementName=cb}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </ComboBox>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>
